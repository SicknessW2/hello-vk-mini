import React, { ChangeEventHandler, SelectHTMLAttributes } from 'react';
import { HasAlign, HasFormStatus, HasRef, HasRootRef } from '../../types/props';
import { GetRef } from '../../types/common';
export interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement>, HasRef<HTMLSelectElement>, HasRootRef<HTMLLabelElement>, HasFormStatus, HasAlign {
    value?: string;
    defaultValue?: string;
    placeholder?: string;
}
export interface SelectState {
    value?: string;
    title?: string;
    notSelected?: boolean;
}
export default class Select extends React.Component<SelectProps, SelectState> {
    constructor(props: SelectProps);
    isControlledOutside?: boolean;
    selectEl?: HTMLSelectElement;
    onChange: ChangeEventHandler;
    setTitle: VoidFunction;
    componentDidUpdate(prevProps: SelectProps): void;
    componentDidMount(): void;
    get value(): string;
    getRef: GetRef<HTMLSelectElement>;
    render(): JSX.Element;
}
