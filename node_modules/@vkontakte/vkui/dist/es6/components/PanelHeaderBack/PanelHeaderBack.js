import React from 'react';
import Icon28ChevronBack from '@vkontakte/icons/dist/28/chevron_back';
import Icon24Back from '@vkontakte/icons/dist/24/back';
import PanelHeaderButton from '../PanelHeaderButton/PanelHeaderButton';
import { ANDROID } from '../../lib/platform';
import usePlatform from '../../hooks/usePlatform';

var PanelHeaderBack = function PanelHeaderBack(props) {
  var platform = usePlatform();
  return React.createElement(PanelHeaderButton, props, platform === ANDROID ? React.createElement(Icon24Back, null) : React.createElement(Icon28ChevronBack, null));
};

export default React.memo(PanelHeaderBack);
//# sourceMappingURL=PanelHeaderBack.js.map